name: Kings Milestone Detector

on:
  workflow_dispatch:

  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write

concurrency:
  group: kings-milestone-detector
  cancel-in-progress: false

jobs:
  check-milestones:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "24"

      - name: Check Kings Milestones
        env:
          MILESTONE_DISCORD_WEBHOOK_URL: ${{ secrets.MILESTONE_DISCORD_WEBHOOK_URL }}
          NEWS_NOTIFICATIONS_ROLE_ID: ${{ secrets.NEWS_NOTIFICATIONS_ROLE_ID }}
        run: node milestones.js

      - name: Save Milestone and Changelog Data
        run: |
          if git diff --quiet -- data/milestones.json data/changelog-queue.json && [ -z "$(git status --porcelain data/milestones.json data/changelog-queue.json)" ]; then
            echo "No Milestone or Changelog Queue changes to save."
            exit 0
          fi

          git config user.name "Kings Logistics Automation"
          git config user.email "actions@users.noreply.github.com"

          git add data/milestones.json
          git add data/changelog-queue.json

          git commit -m "Update Kings Milestone and Changelog data"
          git pull --rebase origin main
          git push origin main
